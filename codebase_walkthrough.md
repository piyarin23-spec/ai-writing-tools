# 個人用AIライティングツールの中身を覗いてみる 〜Streamlit × Gemini APIのシンプル設計〜

## このアプリは何者か

一言でいうと「**Gemini APIを使った、ブラウザで動く文章作成アシスタント**」です。ブログ記事、メール返信、要約、校正・リライト、文体変換、タイトル案、SNS投稿文、翻訳——8つの執筆支援機能が1つのアプリにまとまっています。

面白いのは「**あえて持たせていないもの**」です。データベースなし、ログインなし。入力した内容や生成結果は、そのブラウザを閉じれば消えます。個人が自分のPCでサッと使うための道具として、余計な永続化機構を持たせない、という設計判断がされています。

## 全体構造：Streamlitの「マルチページアプリ」機能に乗っかる

```
app.py                  ← ホーム画面
pages/
  1_📝_ブログ記事作成.py
  2_📧_メール返信文.py
  ...（全8ページ）
utils/
  common.py             ← サイドバー設定・共通ヘルパー
  gemini_client.py      ← Gemini API呼び出しのラッパー
```

Streamlitには「`pages/`フォルダに置いたファイルを自動でサイドバーのメニュー項目にする」という機能があります。ファイル名の先頭についている `1_📝_...`、`2_📧_...` という数字が、そのままメニューの表示順になります。だから各ページは完全に独立したPythonスクリプトで、ボタンを押すたびにStreamlitが**そのファイルを上から下まで丸ごと再実行**します。ページ間で状態を共有する仕組みは `st.session_state`（ブラウザのセッションに紐づく辞書のようなもの）だけ、というのがポイントです。

## 心臓部その1：`utils/common.py` — サイドバーとAPIキー管理

`render_sidebar_settings()` という関数が、全ページの先頭で呼ばれています。これがやっていることは主に3つです。

1. **APIキーの入力欄を出す**。`.env` に `GEMINI_API_KEY` を書いておけばそれが初期値になり、毎回入力しなくて済みます。
2. **使えるモデルの一覧をAPIから動的取得**する。ここが地味に丁寧で、モデルIDをコードにハードコードせず `client.models.list()` で毎回取りに行っています。コメントにも「Geminiのモデルは随時廃止・追加されるため」と書かれていて、Gemini 3.6 Flashのような新モデルが出てもコードを直さず追従できるようになっています。
3. 取得したモデルIDを `st.session_state["model"]` に保存する。

```python
def get_settings() -> tuple[str, str]:
    api_key = st.session_state.get("api_key", "")
    model = st.session_state.get("model", "gemini-3.6-flash")
    return api_key, model

def require_api_key() -> bool:
    api_key, _ = get_settings()
    if not api_key:
        st.warning("サイドバーにGemini APIキーを入力してください。")
        return False
    return True
```

各ページはこの `require_api_key()` を送信ボタン処理の入り口で呼ぶだけで、「APIキー未設定なら警告を出して処理を止める」というガードを実現しています。全ページ共通の作法です。

## 心臓部その2：`utils/gemini_client.py` — Gemini SDKの薄いラッパー

生の `google-genai` SDKを直接あちこちで呼ばず、この1ファイルに集約しています。中身は驚くほどシンプルで、関数は実質2つ。

- `generate_stream(...)` — 生成されたテキストを**チャンクごとにyield**する。Streamlitの `st.write_stream()` と組み合わせると、ChatGPTのように文字が少しずつ流れて表示される、あの体験になります。
- `generate_text(...)` — 一括で文字列を返す版（ストリーミングしない）。

そしてどちらも、SDKが投げてくる例外を全部 `GeminiError` という独自例外に包んでいます。

```python
except Exception as e:
    raise GeminiError(f"Gemini APIの呼び出しに失敗しました: {e}") from e
```

これにより各ページ側は `GeminiError` だけを catch すれば良く、「APIエラーでアプリが真っ白にクラッシュする」ことを防いでいます。

## 各ページの「型」：文章要約ページを例に

`pages/3_📄_文章要約.py` が一番シンプルな例として紹介されています。どのページもこの流れをコピーして作られています。

1. `st.set_page_config()` でページ設定
2. `render_sidebar_settings()` でサイドバーを描画
3. `st.form(...)` でフォームを組み立てる（要約なら「長さ」「形式」「残したい情報」など）
4. 送信されたら `require_api_key()` でチェック
5. **プロンプトをf文字列で組み立てる**
6. `st.write_stream(generate_stream(...))` でストリーミング表示
7. 結果をページ専用の `st.session_state["summary_result"]` に保存（再実行後も表示が消えないように）
8. `st.download_button(...)` でテキストとしてダウンロード可能にする

プロンプトの組み立て方が象徴的です。

```python
prompt = f"""以下の文章を要約してください。

# 要約対象の文章
{text}

# 要約の長さ
{length}

# 出力形式
{output_format}

# 特に残したい情報
{keep_info or "指定なし（重要なポイントを優先してください）"}

余計な前置き（「以下に要約します」等）は不要です。要約結果のみを出力してください。
"""
```

Markdownの見出し（`# 要約対象の文章`）でセクションを区切って、フォーム入力をそのまま埋め込む——これがこのプロジェクト全体のプロンプト設計方針です。別ファイルにテンプレートエンジンを用意するようなことはせず、「1機能=1ファイルに、プレーンなf文字列でプロンプトを書く」というシンプルさを意図的に貫いています。

## ブログ記事作成ページとの違い

`pages/1_📝_ブログ記事作成.py` も基本構造は同じですが、入力項目（トピック・キーワード・想定読者・トーン・文字数・構成）が増え、プロンプトの `temperature`（生成のランダム性）が `0.8` と高めに設定されています。一方、要約ページは `temperature=0.3` と低めです。これは狙って使い分けられていて、

- **要約・翻訳・校正**のような「正確さ・再現性」が大事なタスク → temperatureを低く
- **ブログ記事作成**のような「創造性・表現の幅」が欲しいタスク → temperatureを高く

という考え方が各ページのコードに表れています。

## まとめ：このコードから読み取れる設計思想

| 判断 | 理由 |
|---|---|
| DB・認証なし | 個人のローカルツールに過剰な仕組みは不要という割り切り |
| モデルIDをハードコードしない | Geminiのモデルが頻繁に入れ替わるため、APIから動的取得 |
| プロンプトをf文字列で直書き | 1機能1ファイルの単純さを優先、テンプレート層を導入しない |
| SDK例外を`GeminiError`に統一 | 各ページで雑なtry/exceptを書かずに済む |
| 状態は`st.session_state`のみ | ページ間の複雑な同期を避け、Streamlitの再実行モデルに素直に従う |

全体として「機能を増やす時は新しいパターンを発明せず、既存ページ（特に文章要約ページ）をコピーして作る」という一貫した哲学がCLAUDE.mdにも明記されていて、実際のコードもその通りに統一されています。読みやすさと保守性を、抽象化ではなく「型の反復」で担保しているのが、このコードベースの一番の特徴だと思います。
